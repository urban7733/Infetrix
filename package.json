{
  "name": "infetrix",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "cd frontend && npm run build",
    "vercel-build": "cd frontend && npm ci --include=dev && npm run build"
  }
}
